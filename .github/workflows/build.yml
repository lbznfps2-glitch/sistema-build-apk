name: Build APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Instalar Dependencias do Sistema
        run: |
          sudo apt update
          sudo apt install -y git zip unzip python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev build-essential
          pip install --user --upgrade buildozer cython virtualenv

      - name: Limpar Cache e Buildar
        run: |
          export PATH=$PATH:$HOME/.local/bin
          # Limpa builds antigos que possam estar corrompidos
          buildozer android clean
          # Roda o build aceitando todas as licenças da Google
          yes | buildozer -v android debug
          
      - name: Enviar APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: APK_SISTEMA
          path: bin/*.apk
